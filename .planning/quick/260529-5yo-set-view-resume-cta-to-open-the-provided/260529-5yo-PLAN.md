---
quick_id: 260529-5yo
slug: set-view-resume-cta-to-open-the-provided
status: planned
created: 2026-05-28T22:47:37.272Z
---

# Quick Task 260529-5yo: Set View Resume CTA To Google Drive Link

## Goal

Make the hero `View Resume` CTA open the provided Google Drive resume link in a separate tab.

## Tasks

1. Replace the placeholder `profile.resumeUrl` with the provided Google Drive URL in `src/constants/constants.js`.
2. Verify the existing hero CTA logic treats the URL as public and opens it with `target="_blank"` and `rel="noopener noreferrer"`.
3. Run source assertions and production build verification.

## Acceptance Criteria

- `src/constants/constants.js` contains `https://drive.google.com/file/d/1OJEBwtEi2GmPiqtw2Y-9HJsaa6Z0vk9O/view?usp=drivesdk`.
- `src/constants/constants.js` no longer contains `TODO_PUBLIC_RESUME_URL`.
- `src/components/Hero/Hero.js` keeps `target={isPublicResume ? '_blank' : undefined}`.
- `src/components/Hero/Hero.js` keeps `rel={isPublicResume ? 'noopener noreferrer' : undefined}`.
- `NODE_OPTIONS=--openssl-legacy-provider npm run build` exits 0.
