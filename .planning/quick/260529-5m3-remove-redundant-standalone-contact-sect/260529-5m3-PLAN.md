---
quick_id: 260529-5m3
slug: remove-redundant-standalone-contact-sect
status: planned
created: 2026-05-28T22:32:30.860Z
---

# Quick Task 260529-5m3: Remove Redundant Standalone Contact Section

## Goal

Remove the standalone homepage contact section because the same contact information is already available in the header and footer.

## Tasks

1. Remove `ContactSection` from the active homepage composition.
2. Preserve the `#contact` anchor by moving it to the footer so header navigation and the hero resume fallback do not break.
3. Remove contact-section-only imports, component code, styles, and stale contact section constants that are no longer used.
4. Run source assertions and production build verification.

## Acceptance Criteria

- `src/pages/index.js` no longer imports or renders `ContactSection`.
- The footer renders with `id="contact"`.
- `rg "ContactSection|ContactGrid|ContactCard|ContactLink|ContactStatus|contactHighlights" src` returns no matches.
- `rg "href: '#contact'|id=\"contact\"" src` confirms the anchor still exists.
- `NODE_OPTIONS=--openssl-legacy-provider npm run build` exits 0.
